{
  "name": "crowdfunding-dapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "node": "hardhat node",
    "deploy": "hardhat ignition deploy ./ignition/modules/Crowdfunding.js --network localhost",
    "seed-demo": "hardhat run scripts/seed-demo.js --network localhost",
    "dev": "node scripts/dev-server.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.0",
    "solidity-coverage": "^0.8.17"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
